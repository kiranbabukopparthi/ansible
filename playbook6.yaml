- name: Accessing variables from another file
  hosts: local
  connection: local
  vars_files:
  - variables.yaml
  tasks:
  - name: Access and print variables
    vars:
      COURSE: Ansible
    ansible.builtin.debug:
      msg: "COURSE is : {{ COURSE }}, DURATION is : {{ DURATION }}, TRAINER is : {{ TRAINER }}"